// so today we try to solve problem 

// we have code to sum of digits like sumofdigit(1234) output = 10 

// const sumofdigit=(num)=>{
// const arr = num.toString().split("").map(Number);
//     return arr.reduce((accum,curr)=>{return accum+=curr},0)
    
// }


 const sumofdigit=(num)=>{
    // 
     let val2=JSON.stringify(num)
     console.log(val2);
     
          
 }
console.log(sumofdigit(1234));
